# Constraints:
    # 1 <= g.length <= 3 * 104
    # 0 <= s.length <= 3 * 104
    # 1 <= g[i], s[j] <= 231 - 1

class Solution:
    def findContentChildren(self, g: list[int], s: list[int]) -> int:
        pass

g = [1,2,3]
s = [1,1]
# >>> 1

# g = [1,2]
# s = [1,2,3]
# >>> 2

sol = Solution()
sol.findContentChildren(g,s)
