# Constraints:
    # 1 <= s.length <= 100
    # s consists of lowercase English letters.
    # s[0] != 'i'


class Solution:
    def finalString(self, s: str) -> str:
        pass

s = "string"
# >>> "rtsng"

# s = "poiinter"
# >>> "ponter"

sol = Solution()
sol.finalString(s)
